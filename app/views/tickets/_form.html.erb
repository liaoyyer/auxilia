<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <br>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :class => "tinymce" %>
    <%= tinymce %>
  </div>




<br>
<div class="field">
    <%= f.label :category %>
    <%= f.radio_button :category, 'Software', :checked => true %> Software
    <%= f.radio_button :category, 'Hardware' %> Hardware
    <%= f.radio_button :category, 'Billing' %> Billing
    <%= f.radio_button :category, 'Maintenance' %> Maintenance
    <%= f.radio_button :category, 'Other' %> Other
</div>
















  <%= f.hidden_field :user_id, :value => current_user.id %>





  <br>
  <br>



  <div class="actions">
    <%= f.submit @ticket.new_record? ? 'Create Ticket' : 'Edit Ticket', :class => 'btn btn-success' %>
  </div>


<% end %>
