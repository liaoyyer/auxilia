<nav class="navbar navbar-inverse" role="navigation">
  <div class="container">
    <div class="navbar-header">



          <a class="navbar-brand" href="/admin_dashboard">Auxilia Admin</a>






    </div>

    <ul class="nav navbar-nav navbar-right">


          <% if admin_signed_in? %>



            <li class="dropdown">

          <%= link_to '#', :class => "dropdown-toggle", :'data-close-others' => "true", :'data-hover' => "dropdown", :'data-toggle' => "dropwdown" do %>
                <i class="fa fa-list">
                  <!-- <% @unread_count= @mailbox.inbox(:read => false).count(:id, :distinct => true) %>
                  <% if @unread_count > 0 %>
                  <span class="badge"><%= @unread_count %></span>
                  <% end %> -->
                </i>
          <% end %>

            </li>





            <li class="dropdown">

          <%= link_to '#', :class => "dropdown-toggle", :'data-close-others' => "true", :'data-hover' => "dropdown", :'data-toggle' => "dropwdown" do %>
                <i class="fa fa-bell">
                  <!-- <% @unread_count= @mailbox.inbox(:read => false).count(:id, :distinct => true) %>
                  <% if @unread_count > 0 %>
                  <span class="badge"><%= @unread_count %></span>
                  <% end %> -->
                </i>
          <% end %>

            </li>







            <li class="dropdown">


          <%= link_to conversations_path, :class => "dropdown-toggle", :'data-close-others' => "true", :'data-hover' => "dropdown", :'data-toggle' => "dropwdown" do %>
                <i class="fa fa-envelope">
                  <% @unread_count= @mailbox.inbox(:read => false).count(:id, :distinct => true) %>
                  <% if @unread_count > 0 %>
                  <span class="badge"><%= @unread_count %></span>
                  <% end %>
                </i>
          <% end %>

            </li>


          <% end %>






      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Menu <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">

          <% if admin_signed_in? %>
                <li><%= link_to "Home", admin_dashboard_path %></li>


                  <li><%= link_to "Analysis", admin_dashboard_analytics_path %></li>


                <li><%= link_to 'Messagebox', conversations_path %></li>
                <li><%= link_to "Admin Logout", destroy_admin_session_path, method: :delete %></li>
          <% else %>
            <li><a href="/">User Login</a></li>
            <li><a href="/admins/sign_in">Admin Login</a></li>
          <% end %>
        </ul>
      </li>
    </ul>
  </div>
</nav>





